import React, { Fragment } from 'react';
import Header from './Header';

const Home = props => {
  return (
    <Fragment>
      <Header />
      This is the home page
    </Fragment>
  );
};

export default Home;
